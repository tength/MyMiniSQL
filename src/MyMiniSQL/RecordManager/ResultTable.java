package MyMiniSQL.RecordManager;

import MyMiniSQL.CatalogManager.Attribute;

import java.util.List;

/**
 * use to represent the select result as another table could be operated.
 * also could be thought as a temp table in memory
 */
public class ResultTable {
    //unfinished
    List<Attribute> attributes;
    List<Tuple> tuples;
}
